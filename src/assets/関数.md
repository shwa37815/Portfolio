## 実装コードの備忘録

### 1. プロジェクトの立ち上げとライブラリ追加
```bash
# Viteを使ったReact+TypeScriptプロジェクトの作成
npm create vite@latest portfolio_1 -- --template react-ts

# 開発サーバーの起動
npm run dev

# 必要な機能の追加インストール
npm install react-router-dom  # SPA（画面遷移）用
npm install react-markdown    # マークダウン表示用